# Icon Assets

이 폴더에 앱 아이콘 원본을 넣습니다.

권장 파일:

- `icon.png`

권장 조건:

- 정사각형 PNG
- 1024x1024 이상
- 투명 배경 가능

현재 빌드 스크립트는 `assets/icon.png` 가 있으면 자동으로 앱 아이콘과 런타임 아이콘으로 사용합니다.

시안 파일:

- `icon-concept.svg`
- `icon-preview.html`

권장 기본안은 `icon-concept.svg` 입니다.
